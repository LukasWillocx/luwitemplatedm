% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_colors.R
\name{scale_color_luwi_diverging}
\alias{scale_color_luwi_diverging}
\title{Diverging Color Palette for Data with Midpoint}
\usage{
scale_color_luwi_diverging(theme = my_theme(), n = 11, reverse = FALSE)
}
\arguments{
\item{theme}{A bslib theme object (defaults to my_theme())}

\item{n}{Number of colors to generate (default: 11). Use odd numbers to ensure
a neutral midpoint color}

\item{reverse}{Logical. Flip the palette direction (warm becomes cool and vice versa)}
}
\value{
Character vector of \code{n} hex color codes transitioning from cool (teal)
through neutral (light) to warm (coral/red)
}
\description{
Generates a two-directional gradient palette for data where deviations from a
central value are meaningful (e.g., positive/negative change, hot/cold, above/below average).
}
\details{
The palette transitions: Teal (cold) → Light blue → Neutral warm → Coral → Red-orange (hot).
The neutral midpoint uses your theme's \code{light} color to maintain brand consistency.

\strong{When to use diverging palettes:}
\itemize{
\item Temperature data (cold to hot)
\item Financial change (loss to gain)
\item Survey responses (disagree to agree)
\item Correlation coefficients (-1 to +1)
}
}
\examples{
library(ggplot2)

# Correlation matrix
ggplot(reshape2::melt(cor(mtcars)), aes(Var1, Var2, fill = value)) +
  geom_tile() +
  scale_fill_gradientn(
    colors = scale_color_luwi_diverging(n = 11),
    limits = c(-1, 1)
  )

# Temperature anomalies (reversed: red = hot)
temp_palette <- scale_color_luwi_diverging(n = 9, reverse = TRUE)

# Preview the palette
scales::show_col(scale_color_luwi_diverging())

}
\seealso{
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient2()}} to use with ggplot2,
\code{\link[=scale_color_luwi_sequential]{scale_color_luwi_sequential()}} for one-directional data

Other color-palettes: 
\code{\link{scale_color_luwi_discrete}()},
\code{\link{scale_color_luwi_sequential}()}
}
\concept{color-palettes}
