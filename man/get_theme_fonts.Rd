% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_fonts.R
\name{get_theme_fonts}
\alias{get_theme_fonts}
\title{Extract Fonts from Brand Configuration}
\usage{
get_theme_fonts(theme = my_theme())
}
\arguments{
\item{theme}{A bslib theme object (defaults to my_theme()).}
}
\value{
Named list with three elements:
\describe{
\item{\code{primary}}{Main font family (used for body text, labels, etc.)}
\item{\code{secondary}}{Secondary font family if defined (often used for headings), or \code{NULL}}
\item{\code{all_families}}{Character vector of all font families defined in brand file}
}
If _brand.yml is not found, returns fallback values with "sans" as primary font.
}
\description{
Retrieves font families defined in your _brand.yml file. Returns a structured
list with primary and secondary fonts for easy programmatic access in plots
and custom themes.
}
\details{
This function reads the \code{typography.fonts} section from your package's
_brand.yml file directly. The first font listed becomes \code{primary}, the
second becomes \code{secondary}.

\strong{Expected _brand.yml structure:}

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{typography:
  fonts:
    - family: "Open Sans"
      source: google
    - family: "Merriweather"
      source: google
}\if{html}{\out{</div>}}
}
\examples{
# Get font configuration
fonts <- get_theme_fonts()
fonts$primary    # "Open Sans"
fonts$secondary  # "Merriweather" or NULL

# Use in ggplot2
library(ggplot2)
fonts <- get_theme_fonts()

ggplot(mtcars, aes(mpg, wt)) +
  geom_point() +
  labs(title = "My Plot") +
  theme_minimal(base_family = fonts$primary) +
  theme(
    plot.title = element_text(family = fonts$secondary \%||\% fonts$primary)
  )

# Check what fonts are available
fonts <- get_theme_fonts()
cat("Available fonts:", paste(fonts$all_families, collapse = ", "))

}
\seealso{
\code{\link[=get_theme_colors]{get_theme_colors()}} for extracting theme colors,
\code{\link[=theme_luwi]{theme_luwi()}} to use these fonts in ggplot2 automatically
}
\concept{theme-utilities}
