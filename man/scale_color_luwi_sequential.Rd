% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_colors.R
\name{scale_color_luwi_sequential}
\alias{scale_color_luwi_sequential}
\title{Sequential Color Palette for Continuous Data}
\usage{
scale_color_luwi_sequential(
  theme = my_theme(),
  type = "warm",
  n = 9,
  reverse = FALSE
)
}
\arguments{
\item{theme}{A bslib theme object (defaults to my_theme())}

\item{type}{Palette type:
\itemize{
\item \code{"warm"}
\item \code{"cool"}
\item \code{"green"}
}}

\item{n}{Number of colors to generate (default: 9). More colors = smoother gradient}

\item{reverse}{Logical. Reverse the palette direction (dark to light instead of light to dark)}
}
\value{
Character vector of \code{n} hex color codes
}
\description{
Generates a gradient palette suitable for continuous/ordered data. Choose from
warm (coral-to-red), cool (teal-to-blue), or green (olive-to-dark) gradients.
}
\details{
These palettes are perceptually uniform and designed to work well in both digital
and print contexts. They automatically use your theme's semantic colors as anchor
points to ensure brand consistency.
}
\examples{
library(ggplot2)

# Warm palette for a heatmap
ggplot(faithfuld, aes(waiting, eruptions, fill = density)) +
  geom_tile() +
  scale_fill_gradientn(colors = scale_color_luwi_sequential(type = "warm"))

# Cool palette reversed (dark = high values)
ggplot(economics, aes(date, unemploy)) +
  geom_area(fill = scale_color_luwi_sequential(type = "cool", n = 1))

# Preview the palette
scales::show_col(scale_color_luwi_sequential(type = "green", n = 9))

}
\seealso{
\code{\link[ggplot2:scale_gradient]{ggplot2::scale_fill_gradient()}} and \code{\link[ggplot2:scale_gradient]{ggplot2::scale_color_gradient()}} to use with ggplot2,
\code{\link[=scale_color_luwi_diverging]{scale_color_luwi_diverging()}} for data with a meaningful midpoint

Other color-palettes: 
\code{\link{scale_color_luwi_discrete}()},
\code{\link{scale_color_luwi_diverging}()}
}
\concept{color-palettes}
