% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_ggplot.R
\name{luwi_ggplotly}
\alias{luwi_ggplotly}
\title{Interactive Plotly with Brand Styling for Shiny}
\usage{
luwi_ggplotly(p, theme = my_theme(), base_size = 14, tooltip = "y")
}
\arguments{
\item{p}{A ggplot2 object to convert}

\item{theme}{A bslib theme object (defaults to my_theme())}

\item{base_size}{Base font size in points (default: 14)}

\item{tooltip}{Which aesthetics to display on hover. Can be a character vector
of aesthetic names (e.g., \code{c("x", "y")}) or "all" for all aesthetics.
Default is \code{"y"} to show only y-values}
}
\value{
A plotly htmlwidget ready for Shiny rendering
}
\description{
Converts a ggplot object to an interactive plotly visualization with brand
styling automatically applied. Designed specifically for Shiny dashboards with
transparent backgrounds, custom fonts, and branded grid lines.
}
\examples{
library(ggplot2)
library(plotly)

# Basic interactive plot
p <- ggplot(mtcars, aes(mpg, wt)) +
  geom_point() +
  labs(title = "Fuel Economy vs Weight")

luwi_ggplotly(p)

# Custom tooltips showing multiple values
p <- ggplot(mtcars, aes(mpg, wt, color = factor(cyl))) +
  geom_point(size = 3)

luwi_ggplotly(p, tooltip = c("x", "y", "color"))

# In a Shiny app
\dontrun{
library(shiny)

ui <- page_fluid(
  theme = my_theme(),
  plotlyOutput("plot")
)

server <- function(input, output) {
  output$plot <- renderPlotly({
    p <- ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) +
      geom_point()
    luwi_ggplotly(p, tooltip = c("x", "y", "color"))
  })
}

shinyApp(ui, server)
}

# Custom hover text
p <- ggplot(mtcars, aes(mpg, wt, text = paste("Car:", rownames(mtcars)))) +
  geom_point()

luwi_ggplotly(p, tooltip = "text")

}
\seealso{
\code{\link[=theme_luwi]{theme_luwi()}} for static ggplot version,
\code{\link[plotly:ggplotly]{plotly::ggplotly()}} for underlying conversion function,
\code{\link[plotly:config]{plotly::config()}} for additional plotly configuration

Other ggplot-themes: 
\code{\link{theme_luwi}()}
}
\concept{ggplot-themes}
