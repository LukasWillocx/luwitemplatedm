% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_colors.R
\name{scale_color_luwi_discrete}
\alias{scale_color_luwi_discrete}
\title{Discrete Color Palette for Categorical Data}
\usage{
scale_color_luwi_discrete(theme = my_theme(), n = NULL)
}
\arguments{
\item{theme}{A bslib theme object (defaults to my_theme())}

\item{n}{Number of colors needed. If \code{NULL}, returns all 15 colors. If \code{n > 15},
returns 15 colors with a warning}
}
\value{
Character vector of hex color codes (length = \code{n} or 15 if \code{n} is NULL)
}
\description{
Provides up to 15 distinct, high-contrast colors for categorical variables like
groups, categories, or factors. Colors are carefully selected to be visually
distinguishable while maintaining your brand aesthetic.
}
\details{
The palette prioritizes your brand colors (primary, secondary, success, etc.) for
the first few categories, then adds complementary colors for additional categories.
}
\examples{
library(ggplot2)

# Basic categorical plot
ggplot(mpg, aes(class, hwy, fill = class)) +
  geom_boxplot() +
  scale_fill_manual(values = scale_color_luwi_discrete())

# Specify exact number needed
ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) +
  geom_point(size = 3) +
  scale_color_manual(values = scale_color_luwi_discrete(n = 3))

# Preview all 15 colors
scales::show_col(scale_color_luwi_discrete())

# Warning example: too many categories
scale_color_luwi_discrete(n = 20)  # Returns 15 with warning

}
\seealso{
\code{\link[ggplot2:scale_manual]{ggplot2::scale_color_manual()}} and \code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}} to use with ggplot2,
\code{\link[=scale_color_luwi_sequential]{scale_color_luwi_sequential()}} for many categories (converts to gradient)

Other color-palettes: 
\code{\link{scale_color_luwi_diverging}()},
\code{\link{scale_color_luwi_sequential}()}
}
\concept{color-palettes}
